<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ViewContactsActivity">

    <TextView
        android:id="@+id/textViewLastSyncedContacts"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:gravity="center_horizontal"
        android:textSize="12sp"
        android:textColor="?android:attr/textColorSecondary"
        tools:text="Last Synced: Tue, 02 Jan 2023, 04:50 PM"
        android:layout_alignParentTop="true"
        android:visibility="gone"/> <!-- Initially gone, shown when data loads -->

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewContacts"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="vertical"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        android:layout_below="@id/textViewLastSyncedContacts"
        android:visibility="gone"/> <!-- Initially gone -->

    <ProgressBar
        android:id="@+id/progressBarContacts"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="visible"/> <!-- Initially visible -->

    <TextView
        android:id="@+id/textViewNoContacts"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="@string/no_contacts_synced"
        android:textSize="16sp"
        android:visibility="gone"/> <!-- Initially gone -->

</RelativeLayout>
